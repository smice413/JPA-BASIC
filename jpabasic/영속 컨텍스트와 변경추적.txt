영속 컨텍스트와 변경 추적

find또는 persist는 관리되는 상태로 변경추적 가능.
 따라서 커밋할때 변경사항이 있으면 update 쿼리 돈다.

User = u;
이외의 em.close()함수 후, em.detach(u)<분리하는 것>은 관리될 수 있는 상태(즉, 영속컨텍스트에 담긴상태가 아님)에는
데이터를 변경해도 update쿼리 돌지 않음.